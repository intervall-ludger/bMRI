[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "bmri_fit"
version = "0.1.0"
description = "Native Levenberg-Marquardt relaxation fitting backend for bMRI"
requires-python = ">=3.10"

[tool.maturin]
module-name = "bmri_fit"
